# Keywords that have a value that starts with lower case chars should start with _
# example:
# _generate: generate
# generate: Generate

_create: erzeugen

created: Erzeugt
not_found: NichtGefunden

test:
    _actual: ist
    _expected: soll
    _given: angenommen    # given (gherkin scheme)
    _when: wenn           # when (gherkin scheme)
    _then: dann         # then (gherkin scheme)

    by_id: ViaId
    can_be_found_by_id: DurchIdGefundenWerdenKann
    cannot_be_found_by_id: DurchIdNichtGefundenWerdenKann
    did_find: Findet
    expect: Erwarte
    raises_event: ErzeugtEvent
    from: Von
    for_given: Fuer
    generate: Erzeugt
    if: Wenn
    in: In
    instead_of: Statt
    is_generated_with_correct_values: WirdMitRichtigenWertenErzeugt
    matches: IstIdentisch
    persists: Persistiert
    removes: Entfernt
    returns: Erwiedert
    returns_null: NullErwiedert
    should: Soll
    throws: Wirft
    was_executed: WurdeAusgefuehrt
